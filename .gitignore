# Python virtual environment (recreate with: python -m venv .venv && pip install -r requirements.txt)
.venv/
__pycache__/
*.pyc

# Pipeline outputs (regenerated by running src/staff_id.py -- see README.md)
output/
output_*/

# Local Claude Code runtime state (not project config)
.claude/scheduled_tasks.lock
